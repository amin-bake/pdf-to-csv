# Legacy requirements (for reference only - use service-specific requirements)
Flask==3.0.0
pdfplumber==0.11.8
pandas>=2.2.0
tabula-py==2.9.0
requests==2.32.3
flask-cors==4.0.0

# Note: pdfminer.six has a known vulnerability (GHSA-f83h-ghpp-7wcc)
# This is a pickle deserialization issue in CMap loading.
# Mitigation: Do not load untrusted CMap files, use default CMaps only.
# The vulnerability requires local file system access to exploit.
# A complete fix is pending from the pdfminer.six maintainers.
